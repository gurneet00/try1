// This file is a placeholder for React Native's native_modules.gradle
// It's needed for the build process to work correctly

def reactNativeModules = { rootDir, moduleDir ->
    def modulesMap = [:]

    // Default empty implementation
    return modulesMap
}

ext.reactNativeModules = reactNativeModules
